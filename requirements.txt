# Runtime dependencies (pinned for reproducible builds).
# Source of truth is pyproject.toml; this file is kept for environments
# that install directly via `pip install -r requirements.txt` (e.g. Docker).
#
# Versions verified against PyPI on 2026-07-04. Re-check periodically with:
#   pip index versions <package>
discord.py==2.7.1
jishaku==2.7.5
aiohttp==3.14.1
regex==2026.6.28
python-dotenv==1.2.2
motor==3.7.1
pymongo==4.17.0
asyncpg==0.31.0
deep-translator==1.11.4
Pillow==12.3.0
emoji==2.15.0
pydash==8.0.6
numpy==2.5.0
pytz==2026.2
tzdata==2026.2
pydantic==2.13.4
pydantic-settings==2.14.2
structlog==26.1.0
tenacity==9.1.4